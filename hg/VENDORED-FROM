Vendored from github.com/theagitist/hotglue2 (fork of k0a1a/hotglue2)
branch telaris @ 6ae4973 (bumped from 03181f9)
GPL-3.0; see hg/COPYING and hg/UPSTREAM.md

Note: this is a curated vendor, not a full mirror. Local Telaris patches
(telaris-auth.inc.php and the modified config.inc.php / index.php /
controller.inc.php / common.inc.php / json.php / js/edit.js / js/glue.js) are
retained, and the branch's docs, tests/, docker/, doc/ and tools/ (icon-render
build tooling) are intentionally not vendored. The code and user_code modules
(arbitrary-code injection) are also intentionally NOT vendored into the
auth-gated embed, so their assets are skipped on every bump.

Vendor history:
- dbc7c350: module_mobile.inc.php (mobile scale-to-fit).
- 309eeb7: WebP support (module_image.inc.php, module_page.inc.php, plus the
  IMAGE_WEBP_QUAL define applied surgically into the patched config.inc.php) and
  PeerTube webvideo provider (module_webvideo.inc.php,
  modules/webvideo/webvideo-edit{.js,.min.js}). The accompanying
  tests/ (webp-roundtrip.test.php, peertube-url.test.js) were not vendored.
- e1314db: soundboard module (module_soundboard.inc.php + modules/soundboard/),
  a per-page opt-in that mixes parallel video clips through one Web Audio
  AudioContext. All-new files, no core edits.
- 7416e8d: soundboard YouTube-embed support (module_soundboard.inc.php,
  modules/soundboard/soundboard.js) via the YouTube IFrame Player API for
  cross-origin embeds, alongside the unchanged self-hosted Web Audio path.
- 9aaf491: editor theme/skin + modern icon set + soundboard simplification.
  New module_theme.inc.php (loads css/theme.css in edit mode via render_page_early,
  no core edits) + css/theme.css carrying the Telaris brand skin (Aurora glyphs on
  translucent HUD chips, mint focus signal). Modernised editor icon PNGs across
  img/ and modules/*/ (Lucide glyphs; SVG sources + render.sh live under tools/,
  not vendored). Soundboard embeds now use native controls (provider-agnostic),
  simplifying module_soundboard.inc.php + modules/soundboard/soundboard.js. The
  code/user_code module icon PNGs from this commit were skipped (those modules are
  not vendored).
- 83925d5: in-app image editor (miniPaint). New module_draw.inc.php +
  modules/draw/ (draw-edit.{css,js}, draw.png, and the vendored miniPaint tree
  modules/draw/minipaint/, MIT) - all new files, no core edits. The fork's
  json.php PHP-8 save fix (drop unconditional stripslashes) was NOT taken: the
  embedded json.php is a Telaris-auth-patched file that already carries the
  equivalent PHP-8 fix. (The icon/theme churn seen in the wider main..telaris
  diff netted to no change in the embedded; only the draw module is new.)
- 03181f9: editor UI localization (i18n, EN/ES/PT/FR). New module_i18n.inc.php
  + lang/{en,es,fr,pt}.json + modules/i18n/i18n-edit.{js,css}, and the
  telaris-only module_telaris_locale.inc.php (reads TELARIS_LOCALE, hides the
  language selector). Many files gained t()/$.glue.t() wraps (module_glue,
  module_image, module_page, module_welcome, modules.inc.php, and most
  modules/*/...-edit.js) copied wholesale; miniPaint follows the locale via
  ?lang=. The three patched files (js/edit.js, js/glue.js, json.php) were taken
  from the fork and the local patches re-applied surgically (CSRF upload header,
  CSRF ajaxSetup header, telaris-auth require + telaris_hg_authorize_json).
  user_code module + tests/ skipped as usual.
  Telaris-side wiring (not in hg/): the bridge (telaris-auth.inc.php) defines
  TELARIS_LOCALE from locale_init_strings()['__locale']; edit/index.php opens the
  editor/revisions iframes with &lang=<locale>; each instance's
  user-config.inc.php already has USE_MIN_FILES=false (so the wrapped .js
  sources load, not the stale .min.js twins).
  KNOWN LIMITATION: the embed loads the Telaris instance (for auth), whose own
  t() helper shadows hotglue's guarded t(). So the editor UI localizes via the
  JS layer ($.glue.t + the injected dict, which do not use t()), but hotglue's
  PHP-side server-error strings fall back to their catalog keys. Low impact
  (error paths only); revisit only if those messages need localizing.
- 6ae4973: editor-init regression fix (load order) + server-message i18n.
  REGRESSION FIX (module_i18n.inc.php): the 03181f9 vendor added i18n-edit.js at
  the default prio 5, AFTER edit.js (prio 4). edit.js calls $.glue.t() at load
  time (the colorpicker IIFE), so $.glue.t was undefined: edit.js threw
  "$.glue.t is not a function" and aborted editor init, leaving $.glue.menu
  undefined (no NEW/PAGE menus, no object tools = the embedded editor was
  unusable). Fix loads i18n-edit.js at prio 3 (with glue.js, before edit.js).
  SERVER MESSAGES (fork 375af33, merged as bea696a): module_page.inc.php wraps
  its 4 remaining response() literals in t(), and lang/{en,es,fr,pt}.json each
  gain page.arg_x_invalid / page.arg_y_invalid / page.grid_save_error. Copied
  wholesale (no local patches in those files). The PHP t()-shadow limitation
  above still applies in the embed; this mainly helps the standalone site.

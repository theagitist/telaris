# PHP
*.log
*.cache
vendor/
composer.lock
composer.phar
composer
.phpunit.cache/

# Track vendored frontend libraries (e.g. PDF.js) even though `vendor/` is ignored.
!js/vendor/
!js/vendor/**

# IDE
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# Environment and config
.env
.env.local
config.local.php
config.php

# Federation identity / log keys — generated by bin/init-identity and bin/init-log-key.
# Per-instance, never in source control.
secrets/

# Per-instance user data — MUST never enter source control (PII: editor
# emails/names inside snapshots, uploaded media). Lives in volumes/backups, not
# the repo. Also guarantees it can never reach the published Docker image, which
# CI builds from the tracked tree only.
uploads/
logs/
telaris-snapshots/

# OS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Desktop.ini

# Temporary files
*.tmp
*.temp
*.bak
*.backup

# Node modules (if any)
node_modules/
package-lock.json
yarn.lock

# Build files
dist/
build/
var/

# Planning docs
docs/planning/

; Telaris php-fpm pool overrides.
; clear_env=no lets workers read DB_*/MAIL_*/TELARIS_* from the container
; environment, which config.php consumes via getenv() (so no secret value is
; ever written into a file in the image).
[www]
clear_env = no
